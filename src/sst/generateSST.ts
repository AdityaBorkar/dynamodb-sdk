// TODO: MAKE THIS A CLI COMMAND
// bunx dynamodb-sdk sst --inputPath="/src/fileName.dbSchema" --outputPath="/stacks/fileName.ts" --verbose --help
// "sst-config": "dynamodb-sdk sst --inputPath="/src/fileName.dbSchema" --outputPath="/stacks/fileName.ts" --verbose --help"

export default function generateSST() {
  return
}
